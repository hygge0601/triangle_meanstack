import { ElementRef, TemplateRef, ViewContainerRef, Renderer, ComponentFactoryResolver, Injector, EventEmitter } from '@angular/core';
import 'rxjs/add/operator/take';
import { Direction } from './popover';
export declare class NglPopoverTrigger {
    private element;
    private viewContainer;
    private injector;
    private renderer;
    nglPopover: string | TemplateRef<any>;
    nglPopoverPlacement: Direction;
    nglPopoverTheme: string;
    nglTooltip: string | boolean;
    nglPopoverDelay: any | any[];
    nglInteractive: boolean | string;
    nglOpen: boolean;
    nglPopoverToggled: EventEmitter<boolean>;
    private popover;
    private popoverFactory;
    private componentRef;
    private placement;
    private theme;
    private tether;
    private openDelay;
    private closeDelay;
    private toggleTimeout;
    private interactive;
    private interactiveSubscription;
    constructor(element: ElementRef, viewContainer: ViewContainerRef, injector: Injector, renderer: Renderer, componentFactoryResolver: ComponentFactoryResolver);
    open(delay?: number): void;
    close(delay?: number): void;
    ngOnDestroy(): void;
    private toggle(open, delay);
    private setTether(create?);
    private setPopover();
    private create();
    private readonly projectableNodes;
    private destroy();
}
