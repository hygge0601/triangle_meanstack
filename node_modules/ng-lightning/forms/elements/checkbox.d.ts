import { ChangeDetectorRef, ElementRef, Renderer } from '@angular/core';
import { NglFormElement } from './element';
import { NglFormCheckbox } from './input';
import { NglFormLabelTemplate } from '../form-label';
export declare class NglFormElementCheckbox extends NglFormElement {
    contentEl: NglFormCheckbox;
    labelStr: string;
    labelTpl: NglFormLabelTemplate;
    error: string;
    required: boolean;
    constructor(detector: ChangeDetectorRef, element: ElementRef, renderer: Renderer);
}
